//https://binarysearch.com/problems/Anagram-Partitioning
class AnagramPartitioning {
    static solve(a, b) {
        const result = [];

        if (a.length !== b.length) {
            return result;
        }

        let count = 0;
        for (let i = 0; i < a.length; i++) {
            
        }
    }
}
